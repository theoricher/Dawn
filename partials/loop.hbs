<article class="feed {{post_class}}">
    <div class="feed-calendar">
        <div class="feed-calendar-month">
            {{date published_at format="DD"}}/{{date published_at format="MM"}}/{{date published_at format="YY"}}
        </div>   
    </div>
    <h2 class="feed-title">{{title}}</h2>
    <div class="feed-right">
        {{#if @labs.members}}
            <svg class="icon feed-visibility feed-visibility-{{visibility}}">
                <use xlink:href="#star"></use>
            </svg>
        {{/if}}
        <div class="feed-length">
            {{reading_time minute=(t "1 minute de lecture") minutes=(t "% minutes de lecture")}}
        </div>
    </div>
    <svg class="icon feed-icon">
        <use xlink:href="#chevron-right"></use>
    </svg>
    <a class="u-permalink" href="{{url}}" aria-label="{{title}}"></a>
</article>